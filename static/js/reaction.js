function count_1(type)  {
  // 결과를 표시할 element
  const resultElement = document.getElementById('result_1');
  // 현재 화면에 표시된 값
  let number = resultElement.innerText;
  // 더하기
    number = parseInt(number) + 1;
  // 결과 출력
  resultElement.innerText = number;
}

function count_2(type)  {
  // 결과를 표시할 element
  const resultElement = document.getElementById('result_2');
  // 현재 화면에 표시된 값
  let number = resultElement.innerText;
  // 더하기
    number = parseInt(number) + 1;
  // 결과 출력
  resultElement.innerText = number;
}

function count_3(type)  {
  // 결과를 표시할 element
  const resultElement = document.getElementById('result_3');
  // 현재 화면에 표시된 값
  let number = resultElement.innerText;
  // 더하기
    number = parseInt(number) + 1;
  // 결과 출력
  resultElement.innerText = number;
}

function count_4(type)  {
  // 결과를 표시할 element
  const resultElement = document.getElementById('result_4');
  // 현재 화면에 표시된 값
  let number = resultElement.innerText;
  // 더하기
    number = parseInt(number) + 1;
  // 결과 출력
  resultElement.innerText = number;
}

function count_5(type)  {
  // 결과를 표시할 element
  const resultElement = document.getElementById('result_5');
  // 현재 화면에 표시된 값
  let number = resultElement.innerText;
  // 더하기
    number = parseInt(number) + 1;
  // 결과 출력
  resultElement.innerText = number;
}

function count_6(type)  {
  // 결과를 표시할 element
  const resultElement = document.getElementById('result_6');
  // 현재 화면에 표시된 값
  let number = resultElement.innerText;
  // 더하기
    number = parseInt(number) + 1;
  // 결과 출력
  resultElement.innerText = number;
}
